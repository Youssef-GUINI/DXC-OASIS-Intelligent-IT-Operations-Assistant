export function PerformanceChart({ children }: { children: React.ReactNode }) {
  return (
    <section className="rounded-xl border border-line bg-white p-5 shadow-card">
      {children}
    </section>
  );
}
